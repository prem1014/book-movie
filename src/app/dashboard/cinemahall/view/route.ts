import { Routes } from '@angular/router';
import { ViewCinemaHallComponent } from './component';

export const route: Routes = [
    {
        path: '',
        component: ViewCinemaHallComponent
    }
];
