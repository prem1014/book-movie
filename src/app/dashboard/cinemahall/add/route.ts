import { Routes } from '@angular/router';
import { AddCinemaHallComponent } from './component';

export const route: Routes = [
    {
        path: '',
        component: AddCinemaHallComponent
    }
];
