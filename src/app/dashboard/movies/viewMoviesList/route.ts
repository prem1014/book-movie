import { Routes } from '@angular/router';
import { ViewMoviesListComponent } from './component';

export const route: Routes = [
    {
        path: '',
        component: ViewMoviesListComponent
    }
];
